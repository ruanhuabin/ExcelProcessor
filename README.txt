A python-based tool that use to process excel data.

Dependency:
Python 2.7+
Openpyxl library that used to process excel format data
  on windows10 platform, when finish installing Python2.7+, use: pip install openpyxl to install the openpyxl package
